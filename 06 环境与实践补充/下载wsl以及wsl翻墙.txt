wsl --list
wsl --unregister Ubuntu
wsl --list
wsl --version
wsl --install -d Ubuntu-22.04 --web-download
mkdir D:\wsl
wsl --export Ubuntu-22.04 D:\1.tar
wsl --unregister Ubuntu-22.04
wsl --import Ubuntu-22.04 D:\wsl D:\1.tar --version    2
ubuntu2204.exe  config  --default-user  wsl
rm D:\1.tar


cd myenv
sudo python3 -m venv master_env
source myenv/bin/activate # The activate file is deliberately not executable because it must be sourced.

ubuntu2204.exe  config  --default-user  root


#########################################
国内：Clash for Windows 下载服务模式（关掉360）- TUN 模式 - 打开终端
curl -O https://raw.githubusercontent.com/firedrakeproject/firedrake/refs/tags/2025.4.0.post0/scripts/firedrake-configure
sudo apt install $(python3 firedrake-configure --show-system-packages)

git clone --depth 1 --branch $(python3 firedrake-configure --show-petsc-version) https://gitlab.com/petsc/petsc.git
cd petsc
python3 ../firedrake-configure --show-petsc-configure-options | xargs -L1 ./configure
make all
export OMPI_ALLOW_RUN_AS_ROOT=1
export OMPI_ALLOW_RUN_AS_ROOT_CONFIRM=1
make check

cd ..
sudo python3 -m venv firedrake_venv
source firedrake_venv/bin/activate
export OMPI_ALLOW_RUN_AS_ROOT=1
export OMPI_ALLOW_RUN_AS_ROOT_CONFIRM=1
export $(python3 firedrake-configure --show-env)
echo 'Cython<3.1' > constraints.txt
export PIP_CONSTRAINT=constraints.txt
pip cache remove petsc4py
pip install $PETSC_DIR/src/binding/petsc4py
pip install -r ./firedrake/requirements-build.txt
pip install --no-build-isolation --no-binary h5py --editable './firedrake[check]'

echo $OMPI_ALLOW_RUN_AS_ROOT
echo $OMPI_ALLOW_RUN_AS_ROOT_CONFIRM



齿轮 - 命令面板 - >User Settings json - "PYTHONPATH": "${workspaceFolder}${pathSeparator}${env:PYTHONPATH}" 指的是在vscode中改变默认PYTHONPATH工作目录，env:PYTHONPATH是电脑中原来的PYTHONPATH环境变量，未设置则为空