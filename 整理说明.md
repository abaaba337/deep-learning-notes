# 整理说明

## 合并与目录迁移

| 原位置 | 新位置 | 处理 |
|---|---|---|
| 大模型入门资料中的反向传播 PDF | 01 神经网络与反向传播 | 原文件保留 |
| Notes 与大模型入门资料中的 Notes | 02 PyTorch与计算机视觉 | 保留 GPU 增补版本，合并配套资产，152 个单元格顺序不变 |
| DL 学习笔记与大模型入门资料中的 Transformer | 03 Transformer与大模型入门 | 保留 AI 对齐增补，60 个单元格顺序不变 |
| llm_interview_note | 04 大模型专题与面试 | 保留专题正文、来源和网站入口，按现有文件重建导航 |
| 数据结构与算法 | 05 数据结构与算法 | 保留题目正文 |
| 根目录 Word/文本、环境检查与升级说明 | 06 环境与实践补充 | 集中补充入口，Word/文本原文保留 |

三块资料中 145 份完全相同的配套文件副本已经合并；内容互补的笔记保留，不按标题相似度删除。空的历史目录和编辑器个人配置不进入新仓库。

## 就地纠正

- 修正 MSE/MAE 的平均定义、反向传播与参数更新的区别、CrossEntropyLoss 输入、ToTensor 缩放条件、DataLoader 批次数与 forward/hooks 说明。
- 修正多头注意力所有头重复同一投影、LayerNorm 的方差定义与 epsilon 位置、交叉注意力错误因果掩码及 embedding 设备处理。
- 修正 GPU 推断到 NumPy 的路径、卷积示例的 batch 轴、最大池化的向下取整解释和 AI 对齐英文拼写。
- 更新安装说明中不存在的 torch 2.5 + cu128 组合，以及把旧机器驱动值当作通用要求的描述。

原 Notebook 的历史输出保留；修改过的代码单元格会清除旧输出，避免显示与当前代码不符的结果。未实现的论文实验仍保留空代码单元格。未声称所有笔记及第三方论文已逐句审定，也未执行完整训练或验证外部网站可用性。

## 数据与运行限制

DRIVE 数据原本缺失；笔记中明确标记获取位置。语义分割权重需先训练和保存。历史股票预测参考目录原样保留，存在数据划分与自定义门控实现局限，不应据此解读为经过验证的预测系统。

## 章节布局与图片整理

- 01–03 统一使用 `notes.ipynb`。01 是原 PDF 的阅读入口；02–03 重排主题块，保留各块正文内容和代码。
- 02：FNN/MNIST → OpenCV → CNN → U-Net/语义分割 → 图像去噪 → RNN/LSTM。
- 03：注意力导论 → token/嵌入/位置编码 → 编码器/解码器 → 训练目标 → 训练稳定性 → 高效微调 → 压缩 → 强化学习基础 → 对齐 → 推理与评估。
- 注意力导论和强化学习基础从 02 移入 03 对应位置；去除没有源内容或输出的空单元格，增加主题目录和前后章链接。
- 每章配图统一进入 `images/`；按正文和代码的实际路径核对，删除 16 张未引用图片，合并 8 份同内容副本。数据、视频和模型权重保留。
- 回归测试使用稳定单元格 ID，避免主题移动后读取错误的代码单元格。
